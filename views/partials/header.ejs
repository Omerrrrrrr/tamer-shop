<header class="bg-white/95 backdrop-blur border-b border-slate-200 shadow-sm">
  <div class="max-w-6xl mx-auto px-4 py-3 flex flex-col gap-3 md:flex-row md:items-center md:justify-between">
    <!-- Logo -->
    <a href="/" class="flex items-center gap-3">
      <div class="w-12 h-12 rounded-2xl bg-gradient-to-br from-indigo-600 via-cyan-500 to-emerald-400 text-white flex items-center justify-center text-lg font-bold shadow-lg ring-2 ring-white/70">
        Gİ
      </div>
      <div>
        <p class="text-lg font-semibold text-slate-900 tracking-tight">Güllüce İletişim</p>
        <p class="text-xs text-slate-500">Türkiye'nin her yerine hızlı kargo</p>
      </div>
    </a>

    <!-- Arama (mobilde full-width) -->
    <form
      action="/products"
      method="GET"
      class="flex w-full md:flex-1 max-w-lg items-center gap-2 rounded-xl border border-slate-200 bg-slate-50 px-3 py-2">
      <span class="text-slate-400 text-sm">🔍</span>
      <input
        type="text"
        name="q"
        value="<%= typeof searchQuery !== 'undefined' ? searchQuery : '' %>"
        placeholder="Ürün veya kategori ara"
        class="flex-1 bg-transparent text-sm text-slate-700 placeholder-slate-400 focus:outline-none"
      />
      <button type="submit" class="text-xs font-semibold text-indigo-600">Ara</button>
    </form>

    <div class="flex items-center gap-2 md:w-auto">
      <!-- Sepet -->
      <a href="/cart" class="pill text-slate-700 text-sm justify-center md:w-auto">
        <span class="text-lg">🛒</span>
        <span><%= cartCount || 0 %> ürün</span>
      </a>

      <% if (currentUser) { %>
        <div class="hidden md:flex items-center text-xs text-slate-500 px-2">
          👤 <span class="ml-1 font-semibold text-slate-700"><%= currentUser.name %></span>
        </div>
        <a href="/auth/logout" class="pill text-xs text-slate-700">Çıkış</a>
      <% } else { %>
        <a href="/auth/login" class="pill text-xs text-slate-700">Giriş</a>
        <a href="/auth/register" class="pill text-xs bg-indigo-600 text-white border-indigo-600 hover:bg-indigo-700">
          Kayıt Ol
        </a>
      <% } %>
    </div>
  </div>

  <nav class="border-t border-slate-200">
    <div class="max-w-6xl mx-auto px-4 py-3 flex flex-wrap gap-3 text-sm text-slate-600">
      <a href="/" class="hover:text-indigo-600">Ana Sayfa</a>
      <a href="/products" class="hover:text-indigo-600">Ürünler</a>
      <a href="/cart" class="hover:text-indigo-600">Sepet</a>
      <a href="/about" class="hover:text-indigo-600">Hakkımızda</a>
      <a href="/contact" class="hover:text-indigo-600">İletişim</a>
      <span class="ml-auto text-xs text-slate-400">
        16:00'a kadar verilen siparişler aynı gün hazırlanır
      </span>
    </div>
  </nav>
</header>
