<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <title>Telefon Aksesuarları</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="bg-gray-100">

    <%- include('partials/header') %>

    <section class="bg-gradient-to-r from-indigo-500 to-purple-600 text-white py-20 text-center">
      <h1 class="text-4xl font-bold mb-4">
        Toptan Telefon Aksesuarları Mağazası
      </h1>
      <p class="text-lg opacity-90">
        Kılıf, cam koruyucu, şarj cihazı ve daha fazlası için modern, hızlı ve güvenilir altyapı.
      </p>
    </section>

    <main class="max-w-6xl mx-auto py-10 px-4">
      <h2 class="text-xl font-semibold mb-6">Öne Çıkan Ürünler</h2>

        <div class="grid gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
            <% products.forEach(product => { %>
                <article class="bg-white rounded-lg shadow p-4 flex flex-col">
                <div class="bg-gray-200 rounded mb-3 aspect-[4/3]"></div>
                <h2 class="font-semibold mb-1"><%= product.name %></h2>
                <p class="text-sm text-gray-500 mb-3"><%= product.desc %></p>
                <button class="mt-auto bg-indigo-600 text-white text-sm py-2 rounded hover:bg-indigo-700">
                    Sepete Ekle
                </button>
                </article>
            <% }) %>
        </div>
    </main>

    <%- include('partials/footer') %>
  </body>
</html>