<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <title>Admin Giriş | Telefon Aksesuarları</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="bg-slate-50 text-slate-900">
    <div class="min-h-screen flex items-center justify-center px-4">
      <div class="card glass w-full max-w-md">
        <h1 class="text-2xl font-bold mb-6 text-center">Admin Giriş</h1>

        <% if (error) { %>
          <p class="mb-4 text-sm text-red-600 bg-red-50 border border-red-100 rounded-lg px-3 py-2"><%= error %></p>
        <% } %>

        <form method="POST" action="/admin/login" class="grid gap-4">
          <div>
            <label class="block text-sm mb-1 text-slate-700">Kullanıcı Adı</label>
            <input
              type="text"
              name="username"
              class="input"
              required
            />
          </div>

          <div>
            <label class="block text-sm mb-1 text-slate-700">Şifre</label>
            <input
              type="password"
              name="password"
              class="input"
              required
            />
          </div>

          <button
            type="submit"
            class="btn-primary w-full justify-center">
            Giriş Yap
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
