<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <title>Admin Giriş | Telefon Aksesuarları</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-lg rounded-lg p-8 w-full max-w-md">
      <h1 class="text-2xl font-bold mb-6 text-center">Admin Giriş</h1>

      <% if (error) { %>
        <p class="mb-4 text-sm text-red-600"><%= error %></p>
      <% } %>

      <form method="POST" action="/admin/login" class="grid gap-4">
        <div>
          <label class="block text-sm mb-1">Kullanıcı Adı</label>
          <input
            type="text"
            name="username"
            class="w-full border rounded px-3 py-2 text-sm"
            required
          />
        </div>

        <div>
          <label class="block text-sm mb-1">Şifre</label>
          <input
            type="password"
            name="password"
            class="w-full border rounded px-3 py-2 text-sm"
            required
          />
        </div>

        <button
          type="submit"
          class="bg-indigo-600 text-white py-2 rounded text-sm font-medium hover:bg-indigo-700">
          Giriş Yap
        </button>
      </form>
    </div>
  </body>
</html>