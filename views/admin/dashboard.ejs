<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <title>Admin Panel | Telefon Aksesuarları</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body class="bg-slate-50 text-slate-900">
    <%- include('../partials/header') %>

    <section class="gradient-hero border-y border-slate-200/80">
      <div class="max-w-6xl mx-auto px-4 py-8 flex flex-col gap-3">
        <p class="soft-badge">🔐 Admin Panel</p>
        <h1 class="text-3xl font-semibold">Özet ve hızlı aksiyonlar</h1>
        <p class="text-sm text-slate-600 max-w-3xl">
          Ürünleri yönet, stok durumunu kontrol et ve vitrini her zaman güncel tut.
        </p>
        <div class="flex gap-3 flex-wrap">
          <a href="/admin/products" class="btn-primary">Ürünleri Yönet</a>
          <a href="/admin/categories" class="btn-secondary">Kategorileri Yönet</a>
          <a href="/admin/comments" class="btn-secondary">Yorumlar</a>
          <a href="/admin/logout" class="btn-secondary">Çıkış Yap</a>
        </div>
        <% if (typeof newCommentsCount !== 'undefined' && newCommentsCount > 0) { %>
          <div class="rounded-xl border border-amber-200 bg-amber-50 text-amber-800 text-sm px-3 py-2">
            <strong><%= newCommentsCount %></strong> yeni yorum var.
          </div>
        <% } %>
      </div>
    </section>

    <main class="max-w-6xl mx-auto py-8 px-4 grid gap-8">
      <section class="grid gap-4 sm:grid-cols-3">
        <div class="card glass">
          <p class="text-sm text-slate-500">Toplam Ürün</p>
          <p class="text-2xl font-bold text-slate-900 mt-1"><%= products.length %></p>
        </div>
        <div class="card glass">
          <p class="text-sm text-slate-500">Kategori Sayısı</p>
          <p class="text-2xl font-bold text-slate-900 mt-1"><%= categories.length %></p>
        </div>
        <div class="card glass">
          <p class="text-sm text-slate-500">Panel Durumu</p>
          <p class="text-2xl font-bold text-emerald-600 mt-1">Aktif</p>
        </div>
      </section>

      <section class="card glass">
        <div class="flex items-center justify-between mb-3">
          <h2 class="text-xl font-semibold">Ürünler</h2>
          <a
            href="/admin/products"
            class="btn-secondary">
            Ürünleri Yönet
          </a>
        </div>
        <div class="overflow-x-auto">
          <table class="min-w-full text-sm">
            <thead class="text-left text-xs text-slate-500 uppercase">
              <tr>
                <th class="px-3 py-2">ID</th>
                <th class="px-3 py-2">İsim</th>
                <th class="px-3 py-2">Kategori</th>
              </tr>
            </thead>
            <tbody>
              <% products.forEach(p => { %>
                <tr class="border-t border-slate-100">
                  <td class="px-3 py-2"><%= p.id %></td>
                  <td class="px-3 py-2"><%= p.name %></td>
                  <td class="px-3 py-2"><%= p.category %></td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </section>
    </main>
    <%- include('../partials/footer') %>
  </body>
</html>
